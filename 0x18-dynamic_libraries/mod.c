 (15 sloc)  198 Bytes
#include "main.h"
/**
* mod - adds two given integers
*
* @a: first integer
* @b: second integer
*
* Return: sum of the two integers
*/
int mod(int a, int b)
{
int mod;
mod = a % b;
return (mod);
}
