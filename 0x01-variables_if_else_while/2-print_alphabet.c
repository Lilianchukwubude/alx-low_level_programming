#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lily;

	for (lily = 'a'; lily <= 'z'; lily++)
		putchar(lily);
		printf("\n");

	return (0);
}
